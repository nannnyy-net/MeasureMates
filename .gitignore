# --- Laravel ---
/vendor
/node_modules
/.env
.env.*
/public/build/
/public/hot
/public/storage
/storage/*.key
/storage/pail
/phpunit.result.cache
.phpunit.cache

# --- Backup folders (must never be uploaded) ---
backup_before_cleanup/
backup_before_repair/
backup_unused/

